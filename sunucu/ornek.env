# Meridyen bildirim sunucusu — ortam değişkenleri örneği.
# Bu dosyayı KOPYALA, adını .env yap ve doldur. .env depoya girmiyor.

# Servis hesabı anahtarının JSON İÇERİĞİ (tek satır).
# Firebase konsolu > Proje ayarları > Servis hesapları > Yeni özel anahtar üret
MERIDYEN_SERVIS_HESABI={"type":"service_account","project_id":"meridyen-830fb", ... }

# Alternatif: anahtarı dosya olarak tutuyorsan bunu kullan (üsttekini boş bırak)
# GOOGLE_APPLICATION_CREDENTIALS=/tam/yol/servis-hesabi.json

# Veritabanı adresi (varsayılan zaten bu, değiştirmen gerekmiyor)
# MERIDYEN_DB=https://meridyen-830fb-default-rtdb.europe-west1.firebasedatabase.app

# Sağlık ucu portu (barındırma sağlayıcısı genelde kendi verir)
# PORT=8080

# Kaç dakikadan eski kuyruk kayıtları gönderilmeden atılsın
# MERIDYEN_EN_ESKI_DK=30
